{
  "name": "mason.cli",
  "version": "0.0.2",
  "description": "Mason is a lightweight and modular scaffolding tool",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "build-example": "babel example/src -d example/lib",
    "example": "cd example && ./../bin/mason.js test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": { "mason": "./bin/mason.js" },
  "author": "Andrew Ewing",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-0": "^6.22.0",
    "colors": "^1.1.2",
    "es6-promise": "^4.0.5"
  }
}
